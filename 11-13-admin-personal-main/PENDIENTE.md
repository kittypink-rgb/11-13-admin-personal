# Estado actual del proyecto

Buen trabajo con la Tarea 3. El login funciona, la sesion se guarda y los paneles por rol estan activos.

Sin embargo, el proyecto todavia no tiene lo mas importante: la gestion de empleados.

---

## Donde vas

| Tarea | Estado |
|-------|--------|
| Tarea 1 - Login basico | Completada |
| Tarea 2 - Login con roles y localStorage | Completada |
| Tarea 3 - Dashboard y sesiones | Completada |
| Tarea 4 - Agregar empleados | Pendiente |
| Tarea 5 - Listar, editar y eliminar | Pendiente |

---

## Que debes hacer ahora

Lee `tarea-4.md` y empieza por ahi.

El panel admin en tu `index.html` todavia dice:

```html
<p>Tienes acceso total al sistema.</p>
```

Eso es lo que debes reemplazar siguiendo las instrucciones de la Tarea 4.

---

## Orden de trabajo

1. Lee `tarea-4.md` completa antes de tocar el codigo
2. Haz los cambios en `index.html`, `styles.css` y `app.js`
3. Prueba que puedes agregar un empleado como admin
4. Verifica en DevTools que aparece en localStorage
5. Haz commit y push con el mensaje: `tarea-4 completada`
6. Luego abre `tarea-5.md` y repite el proceso

---

## Como hacer commit y push

Cuando termines cada tarea:

```bash
git add .
git commit -m "tarea-4 completada"
git push origin main
```

---

## Recordatorio

El profe puede ver tu repositorio en cualquier momento. Si no hay commits nuevos, se entiende que no has avanzado.

Cualquier duda, revisa primero el archivo de la tarea. Si sigue sin funcionar, pide ayuda.
